{
  "name": "@litert/smtp",
  "version": "0.1.0",
  "description": "The SMTP implement based on LiteRT.",
  "main": "lib/index.js",
  "scripts": {
    "prepare": "npm run rebuild",
    "build": "echo Using TypeScript && tsc -v && tsc -p .",
    "build-watch": "echo Using TypeScript && tsc -v && tsc -w -p .",
    "rebuild": "npm run clean && npm run lint && npm run build",
    "test": "echo See directory src/samples",
    "clean": "rm -rf lib samples",
    "lint": "tslint -p . -c tslint.json"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/litert/smtp.js.git"
  },
  "bugs": {
    "url": "https://github.com/litert/smtp.js/issues"
  },
  "homepage": "https://github.com/litert/smtp.js#readme",
  "keywords": [
    "litert",
    "lrt",
    "smtp"
  ],
  "author": "Angus.Fenying <fenying@litert.org> (https://fenying.net)",
  "license": "Apache-2.0",
  "typings": "lib/index.d.ts",
  "types": "lib/index.d.ts",
  "dependencies": {
    "@litert/core": "^1.0.6"
  },
  "devDependencies": {
    "@types/node": "^12.7.4",
    "typescript": "^3.2.2"
  }
}
